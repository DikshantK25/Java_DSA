public class Binary_Search {    public static void main(String[] args)    {        int[] arr = {1, 2, 3, 4, 5, 6, 7, 8, 9};        int target = 7;        int ans =BS(arr,target);        System.out.println(ans);    }   static int BS(int[] arr, int x) {        int low = 0;  // Starting index        int high = arr.length - 1; // Ending index        while (low <= high) {            int mid = low + (high - low)/2; // Calculate middle index            if (arr[mid] == x) {                return mid;  // If element is present at mid, return mid            }            if (arr[mid] < x) {                low = mid + 1; // If x is greater, ignore left half            }            else {                high = mid - 1;  // If x is smaller, ignore right half            }        }        return -1; // Element not present    }}